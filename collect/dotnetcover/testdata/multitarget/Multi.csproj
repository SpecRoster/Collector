<Project Sdk="Microsoft.NET.Sdk">
  <!-- Fixture only: a project that targets more than one framework. Never
       built — resolveFramework evaluates the property, it does not compile. -->
  <PropertyGroup>
    <TargetFrameworks>net8.0;net9.0</TargetFrameworks>
  </PropertyGroup>
</Project>
